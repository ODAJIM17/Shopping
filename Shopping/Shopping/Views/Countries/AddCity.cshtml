@model Shopping.ViewModels.CityViewModel
@{
    ViewData["Title"] = "Create";
}


<h4>City</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddCity" autocomplete="off">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StateId" />
           <partial name="_City"/>
            <div class="form-group mt-2">
                <input type="submit" value="Create" class="btn btn-sm btn-default" />
                <a asp-action="DetailsState" asp-route-id="@Model.StateId" class="btn btn-sm btn-default"><i class="fa-solid fa-recycle text-navy"></i> Back to List</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
