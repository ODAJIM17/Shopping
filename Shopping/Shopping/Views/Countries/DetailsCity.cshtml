@model Shopping.Data.Entities.City

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

 <partial name ="_CityDetails"/>
<div>
    <a asp-action="EditCity" class="btn btn-sm btn-primary" asp-route-id="@Model?.Id">Edit</a> 
    <a asp-action="DetailsState" class="btn btn-sm btn-success" asp-route-id="@Model?.State.Id">Back to List</a>
</div>
